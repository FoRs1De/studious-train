/**
 * Возвращает разницу двух чисел
 * @param {number} a
 * @param {number} b
 * @returns {number} Разница чисел
 */
function diff(a, b) {
  return a - b;
}

const result = diff(10, 3);

console.log(result);
